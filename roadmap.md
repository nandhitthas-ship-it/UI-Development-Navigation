# Roadmap

## Done
- Mobile-first UI shell, bottom nav, theme tokens, fonts
- Tasks screen: add, edit, delete, complete, keyword search, category filter chips
- Categories overview + per-category screen
- New task + edit task forms with validation
- Style guide / screenshot-ready screen
- Lovable Cloud enabled: tasks + reminder settings tables with per-user access rules
- Sign in (email/password + Google), optional — app works signed out on-device
- Offline-first store: local cache, change queue, last-write-wins sync on reconnect
- Daily reminder settings screen + hourly scheduled job
- Clear "sending isn't set up yet" state instead of faked email delivery

## Open
- [ ] Verify an email sender domain, then wire the reminder email template
